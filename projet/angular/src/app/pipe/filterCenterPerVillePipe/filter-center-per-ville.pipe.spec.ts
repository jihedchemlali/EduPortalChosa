import { FilterCenterPerVillePipe } from './filter-center-per-ville.pipe';

describe('FilterCenterPerVillePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCenterPerVillePipe();
    expect(pipe).toBeTruthy();
  });
});
